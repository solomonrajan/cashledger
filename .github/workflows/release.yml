name: Release

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - uses: gradle/actions/setup-gradle@v4

      - name: Build Release APK
        run: ./gradlew assembleFlossOsmRelease

      - name: Rename APKs
        run: |
          cd app/build/outputs/apk/flossOsm/release/
          VERSION=${{ github.ref_name }}
          # Ensure version starts with 'v' if it doesn't already
          if [[ $VERSION != v* ]]; then
            VERSION="v$VERSION"
          fi
          # Rename the files
          mv app-floss-osm-universal-release.apk "cashledger-${VERSION}-universal.apk" || true
          mv app-floss-osm-arm64-v8a-release.apk "cashledger-${VERSION}-arm64-v8a.apk" || true
          mv app-floss-osm-armeabi-v7a-release.apk "cashledger-${VERSION}-armeabi-v7a.apk" || true

      - name: Upload APK to Release
        uses: softprops/action-gh-release@v2
        with:
          files: app/build/outputs/apk/flossOsm/release/cashledger-*.apk
